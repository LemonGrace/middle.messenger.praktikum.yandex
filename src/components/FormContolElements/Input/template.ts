// language=hbs
export default `
    <input 
			type="{{ type }}" 
			id="{{ name }}" 
			name="{{ name }}" 
			placeholder="{{ placeholder }}" 
			value="{{ value }}"
            accept="{{ accept }}"
			class='form-control__input'
	>
`;
